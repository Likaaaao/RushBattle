# ============================================================
#  RushBattle — Unreal Engine 5 版本控制忽略规则
#  判断标准只有一条：能由引擎自动重建的东西，就不进版本控制。
# ============================================================

# ---------- 引擎自动生成，可删除、可重建 ----------
Binaries/
Build/
DerivedDataCache/
Intermediate/
Saved/
.vs/
.vscode/

# ---------- Visual Studio / Rider 生成物 ----------
*.sln
*.suo
*.opensdf
*.sdf
*.VC.db
*.VC.opendb
*.code-workspace
.idea/

# ---------- 编译产物 ----------
*.obj
*.pdb
*.dll
*.exe
*.lib
*.exp

# ---------- 操作系统垃圾文件 ----------
Thumbs.db
Desktop.ini
.DS_Store

# ============================================================
#  第三方资源包：不纳入版本控制
#
#  Content/Free_Sounds_Pack/ 体积 65 MB，占整个 Content 的 99%，
#  且当前没有被任何蓝图引用（在 Content/Code 下搜不到该路径）。
#  它在本机保留、编辑器里照常能用，只是不提交到仓库。
#
#  换机器 / 重新 clone 后的还原方式：
#    从 Fab 重新下载该音效包，导入到 Content/Free_Sounds_Pack/ 下的同名结构即可。
# ============================================================
Content/Free_Sounds_Pack/

# ---------- 保险丝：防止体积大的音视频源文件误入仓库 ----------
# 注意：UE 导入后的音频是 .uasset（由上面的 *.uasset / LFS 规则处理），
# 这里拦的是尚未导入的原始素材文件。
*.wav
*.mp3
*.ogg
*.flac
*.mp4
*.mov

# ---------- 常见的大体积压缩包 ----------
*.zip
*.7z
*.rar
*.iso
